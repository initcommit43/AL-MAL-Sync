[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "al-mal-sync"
version = "0.1.0"
description = "Bidirectional sync tool between MyAnimeList and AniList"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "initcommit43" }]
requires-python = ">=3.11"
classifiers = [
    "Environment :: Console",
    "Environment :: X11 Applications :: Qt",
    "Intended Audience :: End Users/Desktop",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Utilities",
]
dependencies = [
    "requests>=2.31",
    "PyYAML>=6.0",
    "click>=8.1",
    "croniter>=6.0",
]

[project.urls]
Homepage = "https://github.com/initcommit43/AL-MAL-Sync"
Repository = "https://github.com/initcommit43/AL-MAL-Sync"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-mock>=3.14",
    "ruff>=0.6",
    "pyinstaller>=6.0",
]
gui = [
    "PySide6>=6.7",
]

[project.scripts]
al-mal-sync = "al_mal_sync.cli:main"
al-mal-sync-gui = "al_mal_sync.gui.app:main"

[tool.ruff]
line-length = 100

[tool.ruff.lint]
# Pinned explicitly: ruff's own "no [tool.ruff.lint] table" default rule set
# has changed between minor versions (0.15 -> 0.16 pulled in DTZ/BLE/YTT/etc.
# without warning), which would otherwise make CI's lint step break on an
# unrelated ruff upgrade rather than on anything this project actually
# changed. This is ruff's long-standing documented baseline (pyflakes plus
# pycodestyle errors) -- the set this codebase was actually written against.
select = ["E4", "E7", "E9", "F"]

[tool.pytest.ini_options]
testpaths = ["tests"]
