"""Desktop GUI for al-mal-sync (PySide6). Wraps the same service layer the
CLI uses (sync/runner.py, oauth.py, config.py, mapping/*) -- this package
owns presentation only, no sync/mapping logic of its own."""
